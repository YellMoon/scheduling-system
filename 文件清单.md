# 排课管理系统 v1.2 - 完整文件清单

## 📁 项目结构

```
scheduling-system/
├── public/
│   └── index.html
├── src/
│   ├── main/
│   │   └── main.js
│   ├── types/
│   │   └── index.ts (v1.2 更新)
│   ├── utils/
│   │   └── helpers.ts (v1.2 更新)
│   ├── services/
│   │   └── browserDatabase.ts (v1.2 更新)
│   ├── pages/
│   │   ├── Dashboard.tsx
│   │   ├── StudentList.tsx
│   │   ├── CourseList.tsx
│   │   ├── InstitutionManager.tsx
│   │   ├── ScheduleCalendar.tsx
│   │   ├── RevenueStatistics.tsx (新增)
│   │   ├── FinanceStats.tsx
│   │   └── SystemSettings.tsx
│   ├── App.tsx (v1.2 更新)
│   ├── index.tsx
│   └── index.css
├── package.json (v1.2 更新)
├── tsconfig.json
├── install.bat (v1.2 更新)
├── build.bat
├── package-complete.bat (新增)
├── 更新说明 v1.2.md (新增)
└── README.md
```

## 🔄 v1.2 更新的文件

### 核心文件（必须替换）
1. `src/types/index.ts` - 类型定义（增加统计接口、服务类型等）
2. `src/utils/helpers.ts` - 工具函数（增加统计工具）
3. `src/services/browserDatabase.ts` - 数据库服务（增加统计功能）
4. `src/App.tsx` - 主应用（增加统计菜单）

### 新增页面
5. `src/pages/RevenueStatistics.tsx` - 收入统计页面（全新）

### 配置文件
6. `package.json` - 依赖配置（增加 chart.js）
7. `install.bat` - 安装脚本（更新说明）
8. `package-complete.bat` - 完整打包脚本（新增）

### 文档
9. `更新说明 v1.2.md` - 更新说明文档（新增）

## 📦 安装步骤

### 全新安装
1. 解压完整项目包
2. 双击运行 `install.bat`
3. 运行 `npm run dev` 测试
4. 运行 `build.bat` 打包

### 更新安装（从 v1.1）
1. 备份数据（系统设置 → 导出）
2. 替换上述 9 个文件
3. 删除 node_modules
4. 运行 `npm install --legacy-peer-deps`
5. 测试后重新打包

## 🎯 v1.2 新功能

### 1. 多维度收入统计
- 日期范围筛选
- 按班型统计（柱状图）
- 按来源统计（饼图）
- 按服务类型统计
- 按机构统计
- 月度趋势图（折线图）

### 2. 学生学费统计
- 按学生汇总
- 按班型分解
- 自动分摊计算

### 3. 一对多课程优化
- 学生单独定价
- 课时费计算方式选择
- 自动汇总计算

### 4. 上门服务类型
- 课程设置服务类型
- 统计按服务类型筛选

## 🔧 技术依赖

### 新增依赖
```json
{
  "chart.js": "^4.4.0",
  "react-chartjs-2": "^5.2.0"
}
```

### 安装命令
```bash
npm install --legacy-peer-deps
```

## 📊 统计功能入口

**菜单**：收入统计（新增菜单项）

**功能**：
- 日期范围选择器
- 三个图表按钮（班型/来源/趋势）
- 多维度统计表格
- 学生学费统计表格

## ⚠️ 注意事项

1. **数据兼容性**：v1.2 向下兼容，旧数据自动迁移
2. **图表依赖**：必须安装 chart.js 和 react-chartjs-2
3. **统计条件**：只有"已完成"状态的课程会计入收入
4. **一对多分摊**：默认按学生人数平均分摊

## 🚀 快速测试

安装完成后：
```bash
# 1. 启动应用
npm run dev

# 2. 在浏览器打开 http://localhost:3000

# 3. 测试功能
- 添加几个学生
- 添加不同班型的课程
- 排课并标记为"已完成"
- 查看收入统计

# 4. 打包
build.bat
```

## 📞 技术支持

如有问题，请检查：
1. Node.js 版本（v18+）
2. 依赖是否完整安装
3. 浏览器控制台错误信息
4. LocalStorage 数据是否正常

---

**版本**: v1.2.0  
**更新日期**: 2026-04-10  
**打包时间**: 自动生成
